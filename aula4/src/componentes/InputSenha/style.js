import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container: {
        flexDirection: 'row',
        borderColor: "#D3D3D3",
        borderRadius: 10,
        borderWidth: 1.5,
        padding: 5,
        alignItems: "center",
    },
    input: {
        width: 275,
        height: 45,
        fontSize: 20
    }
})