import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    botao: {
        width: 175,
        height: 65,
        borderWidth: 2,
        borderRadius: 15,
        borderColor: "#808080",
        justifyContent: 'center',
        alignItems: 'center'
    },
    titulo: {
        fontSize: 25,
        fontWeight: "600"
    }
});